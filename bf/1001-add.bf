,>, # Get the two integers from stdin
[<+>-] # Add the integers by incrementing the first cell and decrementing the
second
< # Move ptr back to the first cell
------------------------------------------------. # Subtract 48 to display the
number
